import { Avatar, Form } from 'antd';

interface PostCreateFormProps {}
export default function PostCreateForm({}: PostCreateFormProps) {
  return (
    <Form>
      <Avatar size="large">M</Avatar>
    </Form>
  );
}

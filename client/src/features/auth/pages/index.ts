import { lazy } from 'react';

export const Forgot = lazy(() => import('./funding-approval'));
export const Login = lazy(() => import('./LogIn'));
export const SignUp = lazy(() => import('./SignUp'));
